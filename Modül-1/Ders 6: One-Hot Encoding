<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ders 6: One-Hot Encoding</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 font-sans text-gray-800 antialiased min-h-screen flex flex-col">

<header class="bg-white border-b border-gray-200 py-6 px-8 shadow-xs">
  <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
    <div>
      <span class="text-xs font-semibold text-blue-600 tracking-wider uppercase">Modül 1: Veri Okuryazarlığı • Ders 6</span>
      <h1 class="text-2xl font-bold text-gray-900 mt-1">Kategorik Veri Dönüşümü ve One-Hot Encoding</h1>
    </div>
    <div class="bg-blue-50 text-blue-700 px-4 py-2 rounded-lg font-medium text-sm border border-blue-100">
      Doğru Matris: <span id="score" class="font-bold">0</span> / 6
    </div>
  </div>
</header>

<main class="flex-1 max-w-7xl w-full mx-auto p-6 grid grid-cols-1 lg:grid-cols-12 gap-6">

  <div class="lg:col-span-4 bg-white p-6 rounded-2xl border border-gray-200 shadow-xs flex flex-col justify-between gap-6">
    <div>
      <h2 class="text-lg font-semibold text-gray-900 mb-2">Algoritma Mantığı</h2>
      <p class="text-gray-600 text-sm leading-relaxed mb-4">
        Yapay zeka modelleri doğrudan "Kırmızı", "Mavi" veya "Mobil" gibi metin kelimelerini işleyemez. Bu verilerin sayısal matrislere dönüştürülmesi gerekir. **One-Hot Encoding** yönteminde, her benzersiz kategori için yeni bir sütun açılır. Veri o kategoriye aitse ilgili sütun `1`, diğer tüm sütunlar `0` değerini alır.
      </p>

      <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded-r-xl space-y-3 mb-4">
        <h3 class="text-xs font-bold text-blue-900 uppercase tracking-wider">Kodlama Yol Haritası</h3>
        <p class="text-xs text-blue-800 leading-relaxed">
          Yandaki editörün içine sırayla şu mantığı kurmanız bekleniyor:
        </p>
        <ol class="text-xs text-blue-800 list-decimal list-inside space-y-2 font-medium">
          <li>Sistemimizde üç olası kategori tanımlıdır: `mobil`, `tablet` ve `masaustu`. Başlangıçta hepsini `0` kabul eden bir nesne oluşturun.</li>
          <li>Gelen `cihaz` parametresi `"mobil"` ise nesnenin mobil alanını `1` yapın.</li>
          <li>Gelen parametre `"tablet"` ise tablet alanını `1`, `"masaustu"` ise masaustu alanını `1` yapın.</li>
          <li>Oluşturduğunuz ve güncellediğiniz bu üçlü durum nesnesini geriye `return` edin.</li>
        </ol>
      </div>

      <div class="bg-amber-50 border-l-4 border-amber-500 p-4 rounded-r-xl">
        <h3 class="text-xs font-bold text-amber-900 uppercase tracking-wider mb-1">Kullanılacak Sentez Yapısı</h3>
        <pre class="text-[11px] font-mono text-amber-950 leading-relaxed">let matris = { mobil: 0, tablet: 0, masaustu: 0 };
if (cihaz === "mobil") {
    matris.mobil = 1;
}
return matris;</pre>
      </div>
    </div>

    <div id="feedback" class="p-4 rounded-xl text-sm font-medium text-center hidden"></div>
  </div>

  <div class="lg:col-span-5 bg-gray-900 rounded-2xl border border-gray-800 shadow-lg flex flex-col overflow-hidden min-h-[500px]">
    <div class="bg-gray-950 px-4 py-3 border-b border-gray-800 flex justify-between items-center">
      <span class="text-xs font-semibold text-gray-400 font-mono">one_hot_encoder.js</span>
      <span class="w-2 h-2 rounded-full bg-blue-500"></span>
    </div>

    <div class="flex-1 p-4 font-mono text-xs text-gray-300 flex flex-col bg-gray-950">
      <div class="text-gray-500 mb-2">// Fonksiyon icerisine kategorik donusum kodlarinizi yazin.</div>
      <div class="text-blue-400 font-semibold text-sm">function <span class="text-green-400">kategoriDonustur</span>(cihaz) {</div>

      <textarea id="code-input" class="w-full flex-1 bg-gray-900/40 border border-gray-800 focus:border-blue-500 text-green-400 p-4 font-mono text-xs rounded-lg focus:outline-hidden resize-none my-2 leading-loose tracking-wide" spellcheck="false" placeholder="// 1. Adimdan baslayarak kodunuzu buraya yazin..."></textarea>

      <div class="text-blue-400 font-semibold text-sm">}</div>
    </div>

    <div class="p-4 bg-gray-950 border-t border-gray-800 flex gap-3">
      <button id="help-btn" class="bg-gray-800 hover:bg-gray-700 text-gray-300 font-medium text-xs px-4 py-3 rounded-xl transition cursor-pointer">
        İpucu Ekle
      </button>
      <button id="run-code-btn" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-medium text-xs px-5 py-3 rounded-xl transition cursor-pointer text-center font-bold uppercase tracking-wider">
        Derle ve Testleri Başlat
      </button>
    </div>
  </div>

  <div class="lg:col-span-3 flex flex-col gap-4">

    <div class="bg-white p-4 rounded-2xl border border-gray-200 shadow-xs">
      <h3 class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-2">Test Edilecek Kategoriler</h3>
      <div class="space-y-1 font-mono text-[10px] text-gray-600 max-h-[180px] overflow-y-auto">
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 1: "mobil"</div>
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 2: "masaustu"</div>
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 3: "tablet"</div>
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 4: "mobil"</div>
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 5: "masaustu"</div>
        <div class="p-1 bg-gray-50 rounded">Kullanıcı 6: "bilinmeyen"</div>
      </div>
    </div>

    <div class="flex-1 bg-white border border-gray-200 p-4 rounded-2xl flex flex-col shadow-2xs">
      <h3 class="font-bold text-gray-800 text-xs uppercase tracking-wider border-b border-gray-100 pb-2 mb-2">Veri Ambarı Konsolu</h3>
      <div id="console-list" class="flex-1 overflow-y-auto space-y-1.5 text-[11px] font-mono text-gray-500">
        <div class="italic text-gray-400">Algoritma henüz çalıştırılmadı. Test sonuçları burada listelenecek.</div>
      </div>
    </div>

  </div>
</main>

<footer class="bg-white border-t border-gray-200 py-4 text-center text-xs text-gray-500">
  Veri ve Yapay Zeka Simülasyon Serisi • Ders 6
</footer>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const codeInput = document.getElementById('code-input');
    const runBtn = document.getElementById('run-code-btn');
    const helpBtn = document.getElementById('help-btn');
    const consoleList = document.getElementById('console-list');
    const feedback = document.getElementById('feedback');
    const scoreDisplay = document.getElementById('score');

    const testData = [
      { ham: "mobil", beklenen: { mobil: 1, tablet: 0, masaustu: 0 } },
      { ham: "masaustu", beklenen: { mobil: 0, tablet: 0, masaustu: 1 } },
      { ham: "tablet", beklenen: { mobil: 0, tablet: 1, masaustu: 0 } },
      { ham: "mobil", beklenen: { mobil: 1, tablet: 0, masaustu: 0 } },
      { ham: "masaustu", beklenen: { mobil: 0, tablet: 0, masaustu: 1 } },
      { ham: "bilinmeyen", beklenen: { mobil: 0, tablet: 0, masaustu: 0 } } // Sınır testi (Uyuşmayan kategori durumu)
    ];

    helpBtn.addEventListener('click', () => {
      codeInput.value = `let matris = { mobil: 0, tablet: 0, masaustu: 0 };\nif (cihaz === "mobil") {\n    matris.mobil = 1;\n} else if (cihaz === "tablet") {\n    matris.tablet = 1;\n} else if (cihaz === "masaustu") {\n    matris.masaustu = 1;\n}\nreturn matris;`;
    });

    runBtn.addEventListener('click', () => {
      consoleList.innerHTML = "";
      let correctCount = 0;
      const userCode = codeInput.value.trim();

      if (!userCode) {
        showFeedback("Lütfen önce editör alanına kodunuzu yazın.", "error");
        return;
      }

      try {
        const userFunction = new Function('cihaz', userCode);

        testData.forEach((item, index) => {
          const userResult = userFunction(item.ham);

          const logLine = document.createElement('div');
          logLine.className = "p-1.5 rounded text-[11px] border";

          // Nesne karşılaştırma doğrulaması
          const eslesme = userResult &&
            userResult.mobil === item.beklenen.mobil &&
            userResult.tablet === item.beklenen.tablet &&
            userResult.masaustu === item.beklenen.masaustu;

          if (eslesme) {
            correctCount++;
            logLine.classList.add("bg-green-50", "border-green-100", "text-green-800");
            logLine.innerHTML = `[OK] K${index+1}: "${item.ham}" -> [M:${userResult.mobil}, T:${userResult.tablet}, MS:${userResult.masaustu}]`;
          } else {
            logLine.classList.add("bg-red-50", "border-red-100", "text-red-800");
            const stringCikti = userResult ? JSON.stringify(userResult) : "Null/Undefined";
            logLine.innerHTML = `[HATA] K${index+1}: "${item.ham}" -> Donen Yapı: ${stringCikti}`;
          }
          consoleList.appendChild(logLine);
        });

        scoreDisplay.innerText = correctCount;

        if (correctCount === 6) {
          showFeedback("Tebrikler! Metinsel kategorileri doğrusal matris dizilimlerine (One-Hot) hatasız dönüştürdünüz.", "success");
        } else {
          showFeedback(`Kod çalıştı ancak matris haritalamasında hatalar var. Başarı: ${correctCount}/6. Bilinmeyen veya hatalı kategoriler için tüm alanların 0 kalmasını sağlayın.`, "error");
        }

      } catch (error) {
        showFeedback(`Sözdizimi Hatası: JavaScript nesne atama kurallarını veya koşul bloklarını kontrol edin.`, "syntax");

        const errorLine = document.createElement('div');
        errorLine.className = "p-2 bg-red-900 text-red-200 text-[10px] rounded font-mono";
        errorLine.innerText = `[HATA KONSOLU]: ${error.message}`;
        consoleList.appendChild(errorLine);
      }
    });

    function showFeedback(message, type) {
      feedback.classList.remove('hidden', 'bg-green-100', 'text-green-800', 'bg-amber-100', 'text-amber-800', 'bg-red-100', 'text-red-800');
      feedback.innerText = message;

      if (type === 'success') {
        feedback.classList.add('bg-green-100', 'text-green-800');
      } else if (type === 'error') {
        feedback.classList.add('bg-amber-100', 'text-amber-800');
      } else {
        feedback.classList.add('bg-red-100', 'text-red-800');
      }
    }
  });
</script>
</body>
</html>
